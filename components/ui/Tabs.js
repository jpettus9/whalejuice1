'use client';import { useState } from 'react';export default function Tabs({tabs,initial=0,onChange}){const [i,setI]=useState(initial);return(<div><div className='flex gap-2'>{tabs.map((t,idx)=>(<button key={idx} onClick={()=>{setI(idx);onChange&&onChange(idx);}} className={`px-3 py-1.5 rounded-lg text-sm ${i===idx?'bg-cyan-500 text-black':'bg-white/10'}`}>{t.label}</button>))}</div><div className='mt-3'>{tabs[i]?.content}</div></div>);}